import 'package:flutter/material.dart';

class AppPallete {
  static const Color backgroundColor = Color(0xFF0D1117); // Deep dark
  static const Color surfaceColor = Color(0xFF161B22); // Cards / surfaces

  // Blue-Purple Gradients
  static const Color gradient1 = Color(0xFF1E1E2F); // Deep blue-gray
  static const Color gradient2 = Color(0xFF2A2F4F); // Muted indigo
  static const Color gradient3 = Color(0xFF3F446B); // Soft desaturated purple

  // Borders & Separators
  static const Color borderColor = Color(0xFF2A2F3A);

  // Common Colors
  static const Color whiteColor = Colors.white;
  static const Color greyColor = Color(0xFF9BA1A6);
  static const Color errorColor = Colors.redAccent;
  static const Color transparentColor = Colors.transparent;
}
